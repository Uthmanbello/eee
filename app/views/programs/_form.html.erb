<%= form_with(model: program) do |form| %>
  <% if program.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(program.errors.count, "error") %> prohibited this program from being saved:</h2>

      <ul>
        <% program.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='login-fields'>
    <div class="field login-field text1 padding">
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name, class: 'input-field' %>
      <hr class= 'rule'>    
    </div>

    <div class="field login-field text1 padding">
      <%= form.label :image, style: "display: block" %>
      <%= form.text_field :image, class: 'input-field' %>
      <hr class= 'rule'>    
    </div>

    <div class="field login-field text1 padding">
      <%= form.label :establish, style: "display: block" %>
      <%= form.text_field :establish, class: 'input-field' %>   
    </div>
  </div>

  <div class='form-btn'>
    <div class='edit-btn'>
      <%= form.submit 'Save', class: 'submit-button' %>
    </div>
  </div>

<% end %>
