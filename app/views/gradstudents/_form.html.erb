<%= form_with(model: gradstudent) do |form| %>
  <% if gradstudent.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gradstudent.errors.count, "error") %> prohibited this gradstudent from being saved:</h2>

      <ul>
        <% gradstudent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :service, style: "display: block" %>
    <%= form.text_field :service %>
  </div>

 

  <div class="field login-field text1 padding">
    <%= form.select :level, options_for_select(['100', '200', '300', '400']), { prompt: 'Select Class' }, class: "form-select input-field prompt" %>
  </div>

  <div>
    <%= form.label :num, style: "display: block" %>
    <%= form.number_field :num %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
